# Fixtures

Ten synthetic narratives with explicitly authored expected cards. These are CLI/schema checks, not ASRS training rows or claims of measured model performance. The dark-tug fixture adapts the PRD example. Ops fixtures demonstrate the shared schema; no ops adapter has been trained.
